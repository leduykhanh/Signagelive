<div id={{service.itemId}} class="col-md-2 service-item" style="{{service.customStyle}}">
    {{#link-to service.url }}
        <img class="img-responsive" src={{service.image}} alt="">
        <div class="name">
            {{service.name}}
        </div>
    {{/link-to}}
</div>

{{yield}}
